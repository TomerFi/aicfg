name: AICfg
author: TomerFi
description: "Stub! Don't use this action directly. Read [the documentation](https://github.com/TomerFi/aicfg) instead."
runs:
  using: composite
  steps:
    - name: Stub
      shell: bash
      run: |
        echo 'This is a stub. Read [the documentation](https://github.com/TomerFi/aicfg) instead.'
        exit 1
